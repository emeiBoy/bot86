export const metadata = {
  title: "Bot86 Robotics",
  description: "China Robotics Export Database"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
